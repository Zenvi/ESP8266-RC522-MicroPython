1. KeyA的初始值是多少？对任意一个Sector Trailer Block进行读操作，默认情况下，读到的信息是什么？
2. 程序源码中，我们在尝试读写一个Block时，验证的是密钥A还是密钥B；如何通过修改代码，让RC522在读写M1卡时，验证另外一个密钥？
3. 请简述：单片机→RC522→M1卡的通信模型
4. 怎样操作，才可以彻底地废掉卡中的一整个扇区（知道怎么操作就行了，别真操作）
5. 你手上的卡，序列号是多少（请附图片）？
6. 每一次写卡时，需要向一个Block中写入____个（理论）最大值为____的十进制数；为什么最大值是这个数，请简要回答。